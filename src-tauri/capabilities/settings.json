{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-window",
  "description": "Settings can pick a user-selected file and open the project homepage.",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:allow-open-url"
  ]
}
